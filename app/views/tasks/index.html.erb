<h1>タスク一覧</h1>
<table>
  <tr>
    <th>タイトル</th>
    <th>詳細</th>
  </tr>
<% @tasks.each do |task| %>
  <tr>
    <td><%= task.name %></td>
    <td><%= task.content %></td>
    <td><%= link_to '詳細',task_path(task.id) %></td>
  </tr>
<% end %>
</table>
